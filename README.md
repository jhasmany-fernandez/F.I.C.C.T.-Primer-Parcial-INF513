🟢 dev: para tu código Java, Maven y Spring Boot

🟢 db: con PostgreSQL corriendo y accesible en localhost:5432


docker-compose up --build -d



docker exec -it proyecto-servidor-correos-desktop-dev-1 bash

nwth lztz ljpa vbqp

mvn clean install
mvn spring-boot:run
